1️⃣ What is the difference between var, let, and const?
Ans:var,let and const used to declare variable;
1.var=var can re-declare;
2.let=let can not re-declare in the some scope;
3.const=const can not re declared and re-assined.
2️⃣ What is the spread operator (...)?
Ans:spread operator(...)used passing values to function and copy arrays or objects;
3️⃣ What is the difference between map(), filter(), and forEach()?
Ans:
1.map() it return new array 
2.filter() returns new array based on a condition;
3.forEach() used for loop its just can loop but does not return.
4️⃣ What is an arrow function?
Ans:arrow function is a short and cleaner function.introduced in Es6
it uses the arrow(=>)symbol;

5️⃣ What are template literals?
Ans:template literals  used backtick (``)string.we can dynamically intsert variables(${}) 
